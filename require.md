项目名称：AI 驱动的智能任务协同看板系统

文档版本：v1.0.0

发布日期：2026 年 5 月 17 日

技术栈约束：前端 Vue3+Vue Draggable，后端 Spring Boot3+Spring AI，数据库 PostgreSQL，WebSocket 实时同步，JWT 认证

1\. 项目背景与目标

现代团队协作需要高效的任务管理与可视化追踪工具。本项目旨在开发一个支持看板视图、任务状态流转、成员分配和截止日期提醒的协同工具，并集成 AI 能力提升任务管理效率。

2\. 核心功能需求（EARS 语法）

2.1 用户认证模块

无处不在的需求：智能任务协同看板系统必须提供基于 JWT 的无状态用户认证机制。

事件驱动的需求：当用户提交注册表单时，系统必须验证邮箱格式和密码强度（至少 8 位，包含大小写字母和数字），并发送验证邮件。

事件驱动的需求：当用户提交登录表单时，系统必须验证凭据，生成有效期为 7 天的 JWT 令牌并返回给前端。

事件驱动的需求：当 JWT 令牌过期时，系统必须自动跳转到登录页面，并清除本地存储的令牌。

事件驱动的需求：当用户点击 "退出登录" 时，系统必须清除本地存储的 JWT 令牌并跳转到登录页面。

2.2 项目管理模块

事件驱动的需求：当已认证用户创建新项目时，系统必须创建项目记录并将该用户设为项目管理员。

事件驱动的需求：当项目管理员邀请成员时，系统必须发送包含邀请链接的邮件，并在成员点击链接接受后将其添加到项目成员列表。

状态驱动的需求：当用户处于项目管理员角色时，系统必须允许其修改项目信息（名称、描述）和管理成员权限（管理员 / 普通成员）。

事件驱动的需求：当用户进入项目列表页面时，系统必须显示该用户参与的所有项目，按最近更新时间倒序排列。

2.3 任务看板模块

无处不在的需求：智能任务协同看板系统必须支持自定义看板列的增删改操作。

事件驱动的需求：当用户拖拽任务卡片到不同看板列时，系统必须通过 WebSocket 实时更新所有在线客户端的任务状态。

事件驱动的需求：当用户创建新看板列时，系统必须保存列名称和排序顺序，并实时同步到其他客户端。

事件驱动的需求：当用户删除看板列时，系统必须提示用户确认，并将该列下的所有任务移动到 "待办" 默认列。

事件驱动的需求：当用户点击 "添加任务" 按钮时，系统必须在当前看板列创建一个新的空白任务卡片。

2.4 任务详情模块

事件驱动的需求：当用户点击任务卡片时，系统必须从右侧滑出任务详情侧边栏，显示标题、描述、优先级、截止日期、负责人和附件。

事件驱动的需求：当用户上传附件时，系统必须验证文件类型（图片 / 文档）和大小（不超过 10MB），并保存到服务器。

事件驱动的需求：当用户修改任务详情时，系统必须实时保存更改并通过 WebSocket 同步到其他客户端。

事件驱动的需求：当用户删除任务时，系统必须提示用户确认，并从数据库中删除该任务记录。

2.5 通知提醒模块

无处不在的需求：智能任务协同看板系统必须提供任务截止前 24 小时的邮件和站内信提醒功能。

事件驱动的需求：当任务截止时间距离当前时间不足 24 小时时，系统必须向任务负责人发送提醒邮件和站内信。

事件驱动的需求：当用户点击右上角铃铛图标时，系统必须显示未读站内信列表。

事件驱动的需求：当用户查看站内信时，系统必须将该通知标记为已读。

2.6 AI 智能功能模块

事件驱动的需求：当用户输入任务描述时，系统必须调用大模型 API 自动生成建议的标签和预估工时。

事件驱动的需求：当用户输入自然语言指令（如 "把所有逾期任务标记为紧急"）时，系统必须解析指令并执行相应的批量操作。

事件驱动的需求：当 AI 生成建议时，系统必须显示 "采纳" 和 "拒绝" 按钮，供用户选择。

事件驱动的需求：当用户采纳 AI 建议时，系统必须自动将建议的标签和工时填入任务详情。

3\. 非功能需求

性能需求：看板拖拽操作的响应时间必须小于 100ms，WebSocket 消息同步延迟必须小于 500ms。

安全需求：所有 API 接口必须通过 JWT 认证，用户密码必须使用 BCrypt 加密存储。

兼容性需求：前端必须支持 Chrome、Firefox、Edge 等主流浏览器的最新两个版本。

可扩展性需求：数据库表结构必须包含 JSONB 字段，以支持未来功能扩展。

可靠性需求：系统必须支持数据备份和恢复，确保数据不丢失。

4\. 验收标准

1.所有功能需求必须按照 EARS 语法描述的行为实现；

2.所有 API 接口必须通过 JWT 认证；

3.看板拖拽操作必须实现多端实时同步；

4.任务截止提醒必须在截止前 24 小时准确发送；

5.AI 功能必须能够正确解析自然语言指令并执行相应操作。

 
